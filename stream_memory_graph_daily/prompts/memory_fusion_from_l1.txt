You maintain structured memory for one specific topic. Update existing_memory using the
already-compressed provisional_l1 memory. The provisional L1 is the only semantic input;
do not request or infer details from the original segments. Return the complete fused topic
and summary, plus the smallest incremental operation set needed to produce the final
topic_context and user_memories.

Use the same operation contract as the regular memory fusion stage. Each operation's
source_segment_ids must refer to IDs listed in new_group.source_segment_ids. These IDs are
provenance only; the provisional L1 content is the evidence presented for the decision.

{{SHARED_SEMANTICS}}

Return exactly:
{
  "topic": "complete fused topic",
  "summary": "complete fused summary",
  "operations": [],
  "no_op_reason": "already_present | no_storable_content | null"
}
