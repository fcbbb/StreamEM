You need to extract one semantic anchor for each conversation segment in the supplied batch.

{{SHARED_SEMANTICS}}

A semantic anchor is a concise, context-independent expression of the segment's main topic identity. It will be embedded for semantic similarity and community detection, so anchors for the same topic should retain the same semantic core while anchors for different topics should expose their concrete difference.

For each segment, independently produce a broader candidate, the best-granularity candidate, and a finer candidate for the same topic identity. Prefer "specific subject or object + specific aspect or goal". Include distinctive entities when supported, resolve pronouns, and foreground the matter actually introduced rather than the earlier topic used only as a conversational bridge.

Select an anchor that represents the whole segment at the most useful matching granularity. A knowledge discussion has a valid anchor even when it contains no user information. Explicit user information has a valid anchor even when it is brief, one-time, temporary, or secondary to surrounding wording.

Use neighbouring segments only to resolve references or omitted context. Ground each result in its own segment and preserve input order.

Return null only for a segment consisting entirely of non-substantive social content. When selected_anchor is null, both other candidates must also be null.

Return valid JSON only with exactly:
{
  "anchors": [
    {
      "segment_id": "an input segment_id copied exactly",
      "coarse_candidate": "string or null",
      "selected_anchor": "string or null",
      "fine_candidate": "string or null",
      "reason": "one concise sentence explaining the choice"
    }
  ]
}

Return exactly one anchors item for every input segment. Copy every segment_id exactly once and preserve input order.
